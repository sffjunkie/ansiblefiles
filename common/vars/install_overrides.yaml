---
install_overrides:
  bat:
    install: role

  picom.debian:
    install: role

  picom.archlinux:
    install: package

  alacritty:
    install: role

  inkscape.debian:
    install: role

  inkscape.archlinux:
    install: package

  exa:
    install: role

  docker:
    install: role
