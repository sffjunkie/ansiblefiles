---
- hosts: container_machines
  become: true
  vars_files:
    - ../common/vars/global.yaml
    - ./vars/main.yaml

  roles:
    - role: playbook_packages

    - role: docker
      when: backend == 'docker'

    - role: podman
      vars:
        use_compose: true
      when: backend == 'podman'

    - docker_compose
