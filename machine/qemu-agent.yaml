---
- hosts: qemu
  become: true
  tasks:
    - name: Install QEMU Guest Agent
      package:
        name: qemu-guest-agent
        state: present

    - name: Start service
      ansible.builtin.systemd:
        name: qemu-guest-agent
        state: started
        daemon_reload: true
