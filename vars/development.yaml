---
go:
  install: local
  version: latest

node:
  install: local
  packages:
    - "@types/node"
    - "typescript"
    - "typedoc"

python:
  packages:
    os:
    pypi:
      - pyenv
      - pytest
      - virtualenvwrapper

    pipx:
      - black
      - cookiecutter
      - poetry
      - sphinx
      - tox
